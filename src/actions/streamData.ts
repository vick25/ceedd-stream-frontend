// 'use server';

// const URL = process.env.NEXT_PUBLIC_BACKEND_API_URL || 'http://localhost:8000/api/v1';

// export async function fetchInfrastructure(): Promise<any> {
//     const response = await fetch(`${URL}/infrastructure`);
//     if (!response.ok) {
//         throw new Error('Failed to fetch infrastructure');
//     }
//     return response.json();
// }
